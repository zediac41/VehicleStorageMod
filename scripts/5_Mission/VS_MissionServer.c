// We hook into MissionServer so later we can:
// - validate spawn positions
// - actually create the vehicle on unpack
modded class MissionServer
{
    // We can add helpers here later, for example:
    // VS_SpawnVehicleFromData(VS_StoredVehicleData data, vector pos, vector dir)

    // Nothing functional yet in Stage 1
}
